class Solution:
    def pancakeSort(self, arr: List[int]) -> List[int]:
        result = []
        if arr == arr.sort():
            retru result
        n = len(arr)
        for i in range(10*n):
           flip_arr =  arr[:i+1].reverse() +arr[i+1:]
           result.append[i]
           if flip_arr == arr.sort():
            reurn 
        